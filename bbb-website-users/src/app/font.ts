import { Imperial_Script } from "next/font/google";

export const imperialScript = Imperial_Script({
  weight: "400", // Imperial Script only has one weight: 400
  subsets: ["latin"],
  variable: "--font-imperial",
  display: "swap",
});
